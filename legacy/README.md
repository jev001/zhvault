# Legacy upstream script

`Main.py` is the original single-file exporter from [zanghuaren/ZhiHu-Collection-To-Markdown](https://github.com/zanghuaren/ZhiHu-Collection-To-Markdown). It is kept here for historical reference only and is **not supported**. Use the `zhvault` CLI and code under `src/` for all backup and development work.
